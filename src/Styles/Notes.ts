import { StyleSheet, Text, View, Dimensions } from "react-native";
export const clinics = StyleSheet.create({
  container: {
    height: "100%",
    width: "100%",
    backgroundColor: "#fff",
  },
});
